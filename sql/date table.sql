CREATE TABLE `final_project`.`date` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `year` INT NULL,
  `month` INT NULL,
  `day` INT NULL,
  `hour` INT NULL,
  `price` FLOAT NULL,
  PRIMARY KEY (`id`));

SELECT * FROM final_project.date;